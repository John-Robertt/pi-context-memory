# pi-context-memory 产品文档

## 1. 文档角色与权威边界

本文是 `pi-context-memory` 的产品权威文档，定义产品为谁存在、解决什么问题、向用户提供什么价值、最终应形成什么体验，以及用什么结果判断产品成功。

产品策划、需求取舍、体验设计和验收标准必须以本文为依据。系统架构由 `ARCHITECTURE.md` 定义，它负责把本文的产品目标转化为系统及模块的战略目标，但不能改变本文定义的用户价值和产品边界。

本文只维护目标产品当前有效的产品定义，不记录实施进度、候选技术路线、实验过程或已被替代的结论；这些内容不能定义正式产品需求。本文也不定义模块划分、技术选型、接口、数据结构、存储、上下文策略、重试规则或部署方式。

## 2. 产品定义

`pi-context-memory` 是 **面向 Pi 长任务的自动记忆与成本优化扩展**。

它让用户继续以原有方式使用 Pi，同时使长会话中的目标、约束、决定、当前有效事实和必要证据能够跨越上下文边界持续支持任务，并减少历史内容被重复发送给模型所产生的不必要成本。

用户继续使用 Pi；`pi-context-memory` 专注于 Pi 长任务的连续记忆与完整成本优化。

## 3. 产品愿景

> **让长任务像短任务一样自然连续：用户不需要感知模型上下文边界，不需要手工维护记忆，也不需要为重复历史持续支付不必要的成本。**

产品的最终状态是：用户可以把一个需要长时间、多轮修改和多次上下文换代的任务持续交给 Pi，而不必反复重述目标、约束和既有结论；在达到与 Pi 原生路径同等或更好任务结果的同时，以更低的完整 API 成本完成任务。

## 4. 目标用户

### 4.1 核心用户

核心用户是使用 Pi 完成长任务的个人开发者和工程人员。这些任务通常具有以下一个或多个特征：

- 需要较多轮调查、实现、验证和修正；
- 会产生大量对话、代码阅读和工具结果；
- 任务目标、约束和当前状态需要长时间保持；
- 事实可能被后续决定更新或废弃；
- 单个任务可能跨越多次上下文压缩或工作上下文换代；
- 模型输入成本会随会话增长成为持续负担。

### 4.2 用户不应承担的工作

用户不应为了使用本产品而：

- 学习一套独立于 Pi 的 Agent 使用方式；
- 判断何时保存、压缩、合并或更新记忆；
- 定期重新描述任务背景；
- 手工区分哪些历史应保留、哪些应退出当前上下文；
- 在记忆服务异常时修复内部状态；
- 为任务模型提供记忆系统的管理指令。

## 5. 用户问题

### 5.1 长任务连续性不足

模型能够直接使用的上下文是有限的。长会话经过压缩或上下文换代后，早期目标、约束、精确事实、否定条件和关键证据可能不再稳定可用，导致用户重复说明、模型采用过期决定或任务偏离原目标。

### 5.2 长历史重复产生费用

持续携带和处理不断增长的历史，会使后续每轮模型调用重复为大量旧内容付费。单纯保留完整上下文虽然可能减少失忆，却无法满足长任务的经济性要求。

### 5.3 手工维护记忆破坏使用体验

手工保存、压缩、整理和清理记忆会给用户增加工作，并占用任务模型推进用户任务所需的能力和 token。

### 5.4 多任务之间需要明确边界

用户可能同时进行多个内容相似但结论不同的任务。一个任务中的决定、约束和召回结果不能进入另一个任务，否则记忆增强反而会降低可信度。

## 6. 产品战略目标

`pi-context-memory` 要到达的产品目的地是：

> **在用户继续正常使用 Pi 的前提下，为每个长任务提供自动且隔离的连续记忆，并在任务质量不劣于 Pi 原生路径时，以更低的完整 API 成本完成任务。**

所有产品范围、需求、规则和评价方法都服务于上述战略目标。具体规则应根据其对任务质量、连续性、成本和透明体验的实际贡献持续校准。

产品价值按以下顺序取舍：

1. **任务连续**：产品首先帮助用户持续完成任务，记忆内容围绕任务推进选择。
2. **事实可信**：当前有效事实、硬约束和用户目标不能被过期或不相关记忆替代。
3. **成本更低**：只有在任务结果不劣时，成本下降才构成产品价值。
4. **使用透明**：记忆和优化应自动发生，不能把系统复杂度转移给用户。
5. **失败可退**：增强能力不可用时，用户仍能继续使用 Pi，只是暂时失去部分记忆或成本收益。

## 7. 用户需要获得的核心能力

### 7.1 自动的任务记忆

产品应自动保留支持任务继续推进的信息，包括：

- 用户要达到的目标；
- 验收条件与硬约束；
- 当前有效的决定和事实；
- 已经完成和尚未完成的工作；
- 影响后续行动的失败结果与证据；
- 被后续决定替代、但需要避免再次采用的旧状态。

产品保存的信息用于支持当前任务正确继续。

### 7.2 有界而连续的工作上下文

产品应避免把不断增长的全部历史重复交给模型，同时确保当前任务所需的信息持续存在。用户不需要知道上下文何时换代，也不需要为换代准备摘要。

上下文缩小不能以丢失目标、约束或当前事实为代价。无法同时满足时，应优先保持任务质量。

### 7.3 按需找回早期信息

当当前工作上下文不足以回答问题或继续行动时，任务模型应能在当前任务范围内找回早期细节。重要信息应能够回到可核对的原始会话来源，并以来源支撑摘要内容。

召回是完成任务的辅助能力，不应演变为由用户维护的记忆管理流程。

### 7.4 任务之间的记忆隔离

每个 Pi session 的记忆只服务于该 session。产品不得自动把另一个 session 的历史、决定或召回结果带入当前任务。

当用户通过 Pi 的对话回退、树导航或分支操作改变当前路线时，产品立即以 Pi 的新路线作为唯一记忆范围。新路线记忆准备期间，Pi 原生上下文和压缩能力继续支持任务。

Pi 原生 session 历史、分支关系、回退与压缩保持原有可用性和操作语义。

### 7.5 自动的成本优化

产品应减少长历史在模型调用中的重复成本，并把记忆生成、召回、失败和重试所产生的成本一并纳入产品价值判断。

产品自动选择缓存策略、上下文长度和记忆更新时机，并根据最终任务质量和完整成本优化。token 压缩率和缓存命中率用于解释优化机制。

### 7.6 Pi 原生接续与状态可见

扩展运行期间，界面持续显示当前模型上下文实际采用扩展增强路径还是 Pi 原生记忆与压缩路径，状态以本次实际模型输入为准。

记忆更新失败、超时或暂时不可用时，Pi 原生记忆与压缩接续当前任务，当前可信内容继续生效。增强结果重新通过当前路线核验并实际进入模型输入后，界面同步恢复为增强状态。

产品以可信内容、Pi 持续可用和准确状态表达记忆优化的降级体验。

## 8. 目标产品体验

### 8.1 开始任务

用户按原有方式进入 Pi 并开始任务。产品自动为当前 session 建立独立记忆，不要求用户先创建任务、选择记忆空间或配置记忆策略。

### 8.2 持续推进长任务

随着会话增长，产品在后台维持任务连续性并优化工作上下文。用户继续提出需求、查看结果和修正方向，不需要为了上下文限制中断工作。

### 8.3 更新与废弃事实

用户改变决定，或通过 Pi 对话回退、树导航和分支操作选择路线后，后续工作采用当前路线中的有效决定。新路线增强上下文准备期间，Pi 原生上下文保持任务连续。

### 8.4 找回早期细节

当任务需要早期错误、约束或证据时，模型可以按需找回相关内容并继续工作。用户不需要先知道信息存放在哪里，也不需要浏览记忆数据库。

### 8.5 同时进行多个任务

用户可以使用不同 Pi session 处理相似或冲突的任务。每个 session 的记忆保持独立，用户无需手工设置隔离规则。

### 8.6 增强与 Pi 原生路径切换

扩展运行期间，用户始终能够看到当前实际使用扩展增强上下文还是 Pi 原生记忆与压缩。记忆或成本优化异常时，Pi 原生路径接续任务并显示对应状态；增强结果重新核验并实际采用后，界面恢复为增强状态。

## 9. 产品需求边界

### 9.1 当前能力范围

- 以 Pi 为唯一产品入口，不复制 Pi 的 Agent 产品能力；
- 自动管理 session 内的任务记忆；
- 保持目标、约束、当前事实和关键证据的连续性；
- 正确处理事实更新和旧决定失效；
- 支持当前 session 范围内的按需召回；
- 隔离不同 session 的记忆和召回；
- 在同等任务质量下形成完整 API 成本优势；
- 与 Pi 对话回退、树导航、分支和原生压缩协作，并持续显示实际采用路径；
- 不要求用户或任务模型管理记忆生命周期。

### 9.2 当前范围之外

- 独立于 Pi 的 Agent、CLI 或交互产品；
- 跨 session 自动共享的用户画像和个人记忆；
- 通用知识库、文档管理或企业搜索；
- 面向用户的记忆增删改查工作台；
- 多用户远程 Agent 托管服务；
- 以牺牲任务质量换取最低 token 数的模式；
- 与“记忆系统 + 成本优化”无直接关系的 Agent 基础设施。

## 10. 产品价值评价

判断产品是否正在接近战略目标，应综合观察以下结果。具体评价方法应说明适用任务、比较基线、证据范围和取舍理由：

1. **用户负担不增加**：用户继续使用 Pi，不需要执行记忆管理操作或重复维护任务摘要。
2. **长任务质量不劣**：在相同任务、模型、Provider 和预算边界下，任务完成结果不劣于 Pi 原生路径。
3. **任务状态连续**：经过长会话和多次上下文换代后，目标、约束、当前有效事实和关键证据仍能支持正确行动。
4. **当前路线可信**：事实更新或 Pi 对话回退、树导航和分支操作后，当前行动只采用当前路线中的决定与记忆。
5. **任务记忆隔离**：不同 session 之间不会自动泄漏历史、决定或召回结果。
6. **完整成本更低**：在达到上述质量的任务中，每个成功任务的完整 API 成本低于 Pi 原生路径。
7. **降级可见且任务连续**：增强准备、故障和恢复期间，Pi 原生记忆与压缩继续执行，扩展界面准确显示当前路径。
8. **价值可验证**：产品用任务结果和完整成本说明收益，压缩率、缓存命中率和召回率用于解释结果。

压缩率、缓存命中率、召回率、延迟和后台失败率用于解释上述产品结果；战略目标的整体判断决定产品是否成功。

## 11. 产品决策准则

新增需求或调整方案时，先判断它对战略目标的净贡献：

1. 它改善了哪些长任务结果，依据是什么？
2. 它如何影响任务连续、事实可信、完整成本、使用透明和失败可退之间的优先关系？
3. 它是否保持 Pi 原有使用方式，并严格尊重当前 session 与当前有效任务路线？
4. 它引入了哪些用户负担、系统复杂度、失败成本和长期维护责任？
5. 是否存在更小的现有能力或结构能够取得相同结果？
6. 移除它会损害哪些当前产品结果？

决策依据是对战略目标的整体贡献。具体规则、评价方法和既有方案根据实际结果校准；产品价值、边界或优先级变化时更新本文。
