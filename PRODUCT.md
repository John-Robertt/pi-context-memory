# pi-context-memory 产品文档

## 1. 文档角色与权威边界

本文是 `pi-context-memory` 的产品权威文档，定义产品为谁存在、解决什么问题、向用户提供什么价值、最终应形成什么体验，以及用什么结果判断产品成功。

产品策划、需求取舍、体验设计和验收标准必须以本文为依据。系统架构由 [`ARCHITECTURE.md`](ARCHITECTURE.md) 定义，它负责把本文的产品目标转化为系统及模块的战略目标，但不能改变本文定义的用户价值和产品边界。

本文只维护目标产品当前有效的产品定义，不记录实施进度、候选技术路线或实验过程，也不定义模块、接口、数据结构、存储、阈值、重试规则或部署方式。

## 2. 产品定义

`pi-context-memory` 是**面向 Pi 复杂长任务的自动记忆与成本优化扩展**。

它让用户继续使用 Pi 完成任务，由增强记忆独占扩展启用期间的工作上下文维护和自动压缩，使目标、约束、决定、当前有效事实和必要证据跨越上下文边界持续支持任务，并减少历史内容被重复发送给任务模型产生的成本。

Pi 继续提供 Agent、会话、工具、模型和交互能力；`pi-context-memory` 负责扩展启用后的连续记忆、工作上下文和自动压缩。

## 3. 产品愿景

> **让复杂长任务像短任务一样可靠连续：用户不感知模型上下文边界，不手工维护记忆，并能在固定预算内持续推进任务。**

产品的目标状态是：用户可以把一个需要长时间、多轮调查、工具执行、修改、验证和修正的任务持续交给 Pi。在必要外部服务保持可用的条件下，增强路径可靠完成任务，不因输出规模、输出速度、路线准备或上下文换代中断；任务结果不劣于可比较的 Pi 原生基线，并具有更低的完整 API 成本。

## 4. 目标用户

### 4.1 核心用户

核心用户是使用 Pi 完成复杂长任务的个人开发者和工程人员。这些任务通常具有以下一个或多个特征：

- 需要较多轮调查、实现、验证和修正；
- 会产生大量对话、代码阅读和工具结果；
- 单轮可能包含多个并行或连续工具调用；
- 工具可能快速返回较大输出；
- 任务目标、约束和当前状态需要长时间保持；
- 事实可能被后续决定更新或废弃；
- 单个任务会跨越多次工作上下文换代；
- 模型输入成本会随会话增长成为持续负担。

### 4.2 用户责任边界

正常运行时，用户不需要：

- 学习独立于 Pi 的 Agent 使用方式；
- 判断何时保存、压缩、合并或更新记忆；
- 定期重新描述任务背景；
- 手工区分哪些历史应保留、哪些内容应退出当前上下文；
- 为任务模型提供记忆系统管理指令。

增强记忆的必要外部服务不可用时，系统暂停新的任务模型请求并提供可操作诊断。用户负责恢复配置、凭据或服务；也可以退出当前 Pi，在禁用扩展后重新启动 Pi 原生路径。扩展运行期间不在增强记忆和 Pi 原生上下文之间切换。

## 5. 用户问题

### 5.1 复杂长任务需要可靠连续

模型能够直接使用的上下文有限。长会话经过多轮工具执行和上下文换代后，早期目标、约束、精确事实、否定条件和关键证据可能退出当前输入，导致任务中断、采用过期决定或偏离原目标。

### 5.2 单轮大输出也会突破上下文边界

并行工具调用、快速连续输出和单次大结果可能在一个用户回合内迅速占满上下文。只处理历史轮次而不处理当前回合，无法保证复杂长任务持续运行。

### 5.3 长历史重复产生费用

持续携带不断增长的完整历史，会使后续每轮任务模型调用重复处理旧内容。单纯保留全部上下文不能满足长任务的经济性要求。

### 5.4 手工维护记忆破坏使用体验

手工保存、压缩、整理和清理记忆会增加用户负担，并占用任务模型推进用户任务所需的能力和 token。

### 5.5 多任务和多路线需要明确边界

不同 Pi session 以及同一 session 的冲突 branch 可能包含不同决定。一个任务或路线中的决定、约束和召回结果不能进入另一个任务或路线。

## 6. 产品战略目标

`pi-context-memory` 的产品战略目标是：

> **在用户继续使用 Pi 的前提下，为每个复杂长任务提供自动、隔离、可追溯且可靠的连续记忆；扩展启用期间由增强记忆独占工作上下文和自动压缩，在必要外部服务可用时可靠完成任务，并在任务质量成立后降低完整 API 成本。**

产品价值按以下顺序取舍：

1. **任务可靠完成**：没有独立证据确认的外部阻塞因素时，复杂长任务必须持续到有效完成。
2. **事实可信**：当前目标、硬约束、有效事实和必要证据不能被过期、错误或跨路线记忆替代。
3. **增强路径独占**：扩展启用后，每个任务模型请求要么采用通过核验的增强上下文，要么在发送前被阻断。
4. **任务连续**：多轮工具调用、快速大输出、上下文换代和路线变化不应成为任务中断原因。
5. **使用透明**：正常运行时，记忆维护、当前回合投影和自动压缩由系统完成，内部路线准备不进入用户操作流程。
6. **完整成本更低**：只有可靠性和任务质量成立后，成本下降才构成产品价值。
7. **故障可处理**：必要能力不可用时，系统保护当前 session 和来源，阻断不可信请求，并提供明确的修复或禁用入口。

## 7. 用户需要获得的核心能力

### 7.1 自动的任务记忆

产品自动保留支持任务继续推进的信息，包括：

- 用户目标与验收条件；
- 硬约束和禁止条件；
- 当前有效的决定和事实；
- 已完成和尚未完成的工作；
- 影响后续行动的失败结果与证据；
- 被后续决定替代、但需要避免再次采用的旧状态。

### 7.2 有界而连续的工作上下文

产品将任务模型输入维持在有界规模，同时确保当前任务所需信息持续存在。用户不需要知道上下文何时换代，也不需要准备摘要。

扩展启用后，工作上下文换代只由增强记忆负责，不采用 Pi 自动 compaction 或 tree summary。Pi session 历史继续保存权威会话事实；任务模型输入采用增强记忆形成的有界投影，已有 summary 文本不进入 VLM 或任务模型上下文。

### 7.3 当前回合的大工具结果治理

产品把当前用户回合中的 assistant 工具调用和对应结果作为完整批次处理。小结果可保持原始形态；超过预算的批次形成有界投影，并保留执行状态、错误、否定结果、必要内容和可恢复来源。

投影不能破坏 Provider 所要求的工具调用与结果配对，也不能在来源尚不可恢复时丢弃任务后续需要的信息。

### 7.4 按需找回早期信息

当前工作上下文不足时，任务模型可以在当前 session 和当前 branch 范围内找回早期细节。重要信息能够回到可核对的 Pi session 来源。

召回用于支持任务完成，不形成由用户维护的记忆管理流程。

### 7.5 任务与路线隔离

每个 Pi session 是独立长任务。当前 branch 是该任务当前有效事实路线。对话回退、树导航、分支、fork、clone、resume 和 reload 改变当前路线后，后续请求只采用新路线核验通过的记忆。

Pi session 历史、分支关系和用户操作语义保持权威；增强记忆负责根据这些事实构造模型工作上下文。

### 7.6 单一运行路径与状态可见

扩展启用时只存在增强记忆任务路径。任务模型请求必须携带当前运行代际、精确路线、来源和内容均核验通过的增强上下文证明。

用户状态只展示：

- **增强记忆 · 初始化中**：必要运行实例和记忆模型能力正在建立；
- **增强记忆**：增强运行正常，包含内部路线准备和等待；
- **增强记忆 · 故障**：新任务模型请求已阻断，需要用户处理诊断。

路线准备、队列和等待属于内部状态，不单独展示。故障恢复后仍进入增强记忆路径；Pi 原生路径只在用户退出并禁用扩展后由新的 Pi 运行实例提供。

### 7.7 低负担的记忆模型配置

用户通过 `~/.pi/pi-context-memory.jsonc` 配置产品使用的记忆模型。系统提供包含已有实际纵向证据的 Provider/模型支持范围、`api_key` 和必要连接字段说明的模板。

配置加载、OpenViking readiness 和记忆模型实际能力是独立事实。系统只有在实际能力验证通过后才允许任务模型请求。配置或能力无效时不覆盖用户文件，并给出修复入口。

### 7.8 自动的成本优化

产品减少长历史在任务模型调用中的重复成本，并把记忆生成、召回、重试和故障处理成本一并纳入完整成本。

成本优化服从任务可靠完成和事实可信；不能以更低 token 数掩盖任务中断或质量下降。

## 8. 目标产品体验

### 8.1 开始任务

用户按原有方式进入 Pi。扩展先建立受管 OpenViking 运行实例、验证记忆模型能力并显示“增强记忆 · 初始化中”；增强路径可用后显示“增强记忆”。

### 8.2 持续推进复杂长任务

随着会话增长，产品自动维护长期记忆、有界历史和当前回合投影。多个快速工具调用和大结果由增强路径受控处理，用户继续推进任务而不管理上下文。

### 8.3 更新与废弃事实

用户改变决定或选择新的会话路线后，后续请求只采用当前路线中的有效目标、约束和事实。内部路线准备完成前，请求在增强路径内等待；无法可靠完成时进入故障状态。

### 8.4 找回早期细节

任务需要早期错误、约束或证据时，模型通过统一召回入口展开当前路线来源，用户不需要浏览记忆数据库。

### 8.5 处理故障

必要配置、凭据、记忆模型或 OpenViking 服务不可用时，系统在 Provider 请求发送前阻断任务，保持当前 session 和来源不变，并显示诊断。用户修复后显式重启或重新验证增强服务，再重新提交任务。

需要 Pi 原生行为时，用户退出当前实例、禁用扩展并重新启动 Pi。

## 9. 产品需求边界

### 9.1 当前能力范围

- 以 Pi 为唯一产品入口，不复制 Pi 的 Agent 产品能力；
- 支持能够在发送前完整核验任务请求内容的 Pi 模型 API；
- 以具有权威 session file 的持久化 Pi session 为增强记忆边界；
- 自动管理 session 内的任务记忆；
- 独占扩展启用期间的工作上下文与自动压缩；
- 处理跨轮历史和当前回合大工具结果；
- 保持目标、约束、当前事实和关键证据的连续性；
- 正确处理事实更新和旧决定失效；
- 支持当前 session 和 branch 范围内的按需召回；
- 隔离不同 session 与冲突路线的记忆；
- 在可靠完成和任务质量成立后形成完整 API 成本优势；
- 提供初始化、正常运行、故障和显式恢复体验；
- 不要求用户或任务模型管理日常记忆生命周期。

### 9.2 当前范围之外

- 独立于 Pi 的 Agent、CLI 或交互产品；
- 跨 session 自动共享的用户画像和个人记忆；
- 通用知识库、文档管理或企业搜索；
- 面向用户的记忆增删改查工作台；
- 多用户远程 Agent 托管服务；
- 以牺牲任务可靠性或质量换取最低 token 数的模式；
- 多个记忆系统的选择、适配或运行时切换；
- 扩展运行期间切换到 Pi 原生上下文或压缩；
- 与记忆和成本优化无直接关系的 Agent 基础设施。

## 10. 产品价值评价

产品结果按以下顺序验证：

1. **复杂长任务可靠完成**：在声明支持的任务和环境范围内，未发生经独立观测确认的必要外部服务不可用时，全部有效运行都通过最终任务 checker。
2. **任务质量成立**：目标、约束、当前事实、禁止条件和必要证据支持正确结果。
3. **增强路径独占**：扩展启用期间，已发送的任务模型请求全部携带合法增强证明；不满足条件的请求在 Provider 前被阻断。
4. **当前路线可信**：事实更新和路线变化后，后续行动只采用当前 branch 的决定与记忆。
5. **任务记忆隔离**：不同 session 和冲突 branch 不泄漏历史、决定或召回结果。
6. **当前回合有界**：快速、并行和大输出工具批次保持协议完整、来源可恢复和模型输入有界。
7. **用户负担受控**：正常运行不需要用户管理记忆；故障诊断能直接支持修复或禁用选择。
8. **完整成本更低**：前述可靠性和质量成立的任务中，增强路径的全部 API 账单低于可比较的 Pi 原生基线。
9. **价值可复核**：任务结果、Provider payload、阻断原因、来源恢复和完整账单形成可重复证据。

### 10.1 实际证据与付费验证授权

关键产品结论必须来自实际运行链路：真实 Pi 生命周期、真实受管 OpenViking、用户已配置的实际记忆 Provider/模型、当前任务 Provider/模型、真实工具与文件结果，以及 Provider 返回的 usage 或账单。协议替身只用于确定性故障注入、边界穷举和零请求观测，不能单独证明功能完成、任务质量、记忆模型能力或成本优势。

沿用验证开始时已配置的 Provider、模型和凭据边界执行这些调用，属于正常交付和验证流程；即使产生 API 费用，也不需要逐次向用户申请授权。runner 必须用 manifest 固定调用坐标、样本数、`maxAttempts`、停止条件和账单归属，避免无界调用。

只有验证需要改变任务 Provider、任务模型、记忆 Provider 或记忆模型时，才把变更原因、影响和成本风险交给用户决定。凭据缺失、认证失败、配额或服务不可用属于待诊断或外部阻塞，不视为切换 Provider/模型的授权。

Provider、模型或 API 只有具备对应实际证据时才进入声明支持范围；仅由协议替身覆盖的适配器保持未验证状态，不扩大产品支持矩阵。

可靠性采用以下口径：

```text
eligibleRuns = 未发生经独立观测确认的必要外部服务不可用的运行
completionReliability = 有效完成任务的 eligibleRuns / 全部 eligibleRuns
```

在声明支持的验证范围内，`completionReliability` 必须为 `100%`。内部准备慢、输出大、请求间隔短、队列拥堵、竞态、超时或实现错误都不能归类为外部阻塞。无法独立归因的失败计为产品失败。

成本比较只在可靠性、质量、增强独占和隔离全部成立后进行。压缩率、缓存命中率、召回率、延迟和后台任务结果用于解释机制，不替代产品成功条件。

## 11. 产品决策准则

新增需求或调整方案时，依次判断：

1. 它是否提高复杂长任务的有效完成可靠性？
2. 它是否保持当前 session、当前 branch 和来源事实可信？
3. 它是否确保扩展启用后的任务模型请求只经过增强路径？
4. 它能否处理当前回合的大量、快速和并行工具结果？
5. 它引入了哪些用户负担、失败成本和长期维护责任？
6. 在可靠性和质量成立后，它是否改善完整成本？
7. 是否已有更小的现有结构能够承担同一责任？
8. 移除它会损害哪些当前产品结果？

产品方向由可靠完成、事实可信和单一路径共同约束。不能持续贡献这些结果，或已有更小结构完整承担同一责任的内容，应当移除。
