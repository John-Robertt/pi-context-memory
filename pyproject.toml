[project]
name = "pi-context-memory"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "openviking[local-embed]==0.4.13",
  "xxhash<4", # Keep until the locked OpenViking passes adapter/runtime validation with xxhash 4.
]

[tool.uv]
package = false
