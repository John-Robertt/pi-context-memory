# pi-context-memory 系统架构

## 1. 文档角色与权威边界

本文是 `pi-context-memory` 的系统架构权威文档，定义系统及各模块为到达产品目的地所承担的架构定位、核心目标、职责边界和协作关系。

[`PRODUCT.md`](PRODUCT.md) 是产品目标、用户需求、产品范围和价值评价的权威来源。本文承接产品文档定义系统路线，不重新定义产品价值。

本文不规定具体的数据结构、接口签名、存储格式、阈值、重试次数、部署方式或实现步骤。具体功能、系统、模块、契约、验证与运行文档统一存放在 [`docs/`](docs/README.md)，其归属和生命周期由 [`DOCUMENTION.md`](DOCUMENTION.md) 约束。

本文只维护当前有效的目标架构。实现进度和证据状态由 [`docs/DEVELOPMENT.md`](docs/DEVELOPMENT.md) 维护。

## 2. 整体战略目标

为实现 `PRODUCT.md` 定义的产品目的地，系统战略目标是：

> **在不替代 Pi Agent、会话、工具、模型和交互能力的前提下，为每个受支持的持久化 Pi session 提供自动、隔离、可追溯且可靠的长时记忆；扩展启用期间独占任务模型工作上下文与自动压缩，在必要外部服务可用时可靠完成复杂长任务，并在任务质量成立后降低完整 API 成本。**

这个目标落实为五个相互依赖的架构结果：

1. **可靠任务执行**：在声明支持的环境中，内部等待、并发、输出规模、上下文换代和路线变化不造成任务中断。
2. **事实与路线可信**：任务模型只采用当前 session、当前 branch 和可核验来源中的有效信息。
3. **增强路径独占**：扩展启用期间，每个任务模型请求要么携带合法增强证明发送，要么在 Provider 前被阻断。
4. **有界工作上下文**：跨轮历史和当前回合工具结果共同维持在稳定预算内，并保持来源可恢复和 Provider 协议完整。
5. **完整成本优势**：可靠性和任务质量成立后，系统触发的全部 API 请求总成本低于可比较的 Pi 原生长会话基线。

任务可靠完成和事实可信是增强采用与成本优化的前提。实现不能通过放宽路线、丢弃必要来源或切换运行路径掩盖失败。

## 3. 产品与系统定位

### 3.1 Pi 宿主边界

系统附着于 Pi 扩展生命周期。Pi 继续负责 Agent 循环、会话持久化、工具执行、模型接入和用户交互；本系统负责扩展启用后的记忆、工作上下文、自动压缩、召回和故障闸门。

Pi session 历史、tree、branch 和用户操作语义保持权威。扩展通过 Pi 生命周期与请求 hook 构造任务模型实际输入，并在条件不成立时终止该次 Agent 请求。

Pi 原生上下文与压缩属于扩展未启用的独立运行方式。用户通过退出当前 Pi、禁用扩展并重新启动来选择该方式；同一扩展运行实例不在两种路径之间切换。

### 3.2 最高隔离边界

一个 Pi session 对应一个独立长任务，也是记忆的最高隔离边界。不同 session 之间不自动共享：

- 长时记忆；
- 工作上下文；
- 召回结果；
- 任务状态；
- 故障状态；
- 成本归属。

同一 session 内，当前 branch 表示从会话根到当前 leaf 的事实与决定有效路线。对话回退、树导航、fork、clone、resume 和 reload 以操作后的当前 leaf 重新定义唯一有效路线。

增强记忆只对具有权威 session file 的持久化 Pi session 生效。没有稳定 session file 时无法建立可恢复来源和跨重启身份，新任务模型请求保持阻断。

### 3.3 事实与派生信息边界

Pi session 历史是会话事件身份、顺序和分支关系的权威来源。本系统保存的来源归档是用于恢复和检索的事实副本；Working Memory、OpenViking abstract、索引、工具批次投影和工作上下文均是可重建的派生信息。

派生信息必须绑定当前 session、当前 branch、运行代际和来源。它可以替代历史内容进入任务模型输入，但不能覆盖 Pi 权威历史，也不能成为关键事实不可恢复的唯一载体。

目标、验收条件和硬约束构成任务连续性的稳定锚点。锚点及其变更必须能够回到当前有效路线中的用户来源。

### 3.4 管理责任边界

系统负责：

- 记忆模型能力何时达到可运行条件；
- 来源何时归档和核验；
- 工作上下文何时更新和换代；
- 当前回合工具结果如何保持有界；
- 哪些请求允许进入 Provider；
- Pi 自动 compaction 与 tree summary 如何被禁止，增强工作上下文如何自行保持有界；
- 故障如何锁存、诊断和恢复。

任务模型负责完成用户任务和按需发起来源召回，不管理记忆生命周期。

用户状态只呈现“增强记忆 · 初始化中”“增强记忆”和“增强记忆 · 故障”。路线准备、等待、队列和镜像生命周期属于内部观测，不形成用户状态。

## 4. 架构原则

### 4.1 通过 Pi 扩展提供单一增强路径

Pi 是宿主与控制平面，OpenViking 是记忆与上下文能力提供者，扩展承担双方接口、数据和版本变化的适配成本。项目启动器只管理自己创建的 OpenViking 实例并以启动标识确认所有权。

扩展运行实例从初始化开始只提供增强路径。配置、服务、模型能力、路线或内容核验未满足时，请求保持在 Provider 边界之前。

### 4.2 请求发送以增强证明为前置条件

每个任务模型请求必须绑定：

- 当前受管 OpenViking 运行代际；
- 当前 Pi session 与 session file；
- 当前 leaf 和有序 branch 来源；
- 完整路线指纹；
- 最终工作上下文内容证明。

`context` 阶段负责等待并构造精确结果；最终 Provider 边界再次核验增强证明。任一条件不成立时，系统终止该次 Agent 请求并记录阻断原因。

### 4.3 记忆服务于任务，不成为新的事实权威

长期记忆让退出当前模型输入的信息继续可用。来源、路线与用户决定仍由 Pi session 定义；相似度、摘要和 Working Memory 只提供派生线索和有界投影。

### 4.4 跨轮历史与当前回合共同受预算约束

工作上下文由当前有效 Working Memory、来源核验的跨轮活跃历史、当前用户 prompt 和当前回合工具批次共同组成。目标、约束和有效决定依靠 Working Memory 与用户来源连续性保持，不建立第二套语义提取状态。

assistant 工具调用和对应结果作为不可拆散的协议单元治理。需要缩小时，系统对完整批次形成来源可恢复的有界投影；不支持的内容形态进入故障边界，不以破坏工具协议换取更小输入。

### 4.5 增强记忆独占上下文换代

扩展启用期间，模型工作上下文换代只由增强记忆完成。Pi compaction 一律取消；`tree` 只改变权威 leaf 与 branch，不生成 Pi 原生或扩展 branch summary，也不建立 `branch_summary` entry。

Pi session 保留完整权威历史。已有 compaction 或 branch summary entry 只参与路线身份与边界解释，其 summary 文本不进入 VLM、来源索引或任务模型上下文；增强记忆只从权威 message 来源重建 Working Memory 和有界输入。

### 4.6 故障阻断保护任务可信度

配置无效、必要服务不可用、记忆模型能力失败、来源不可恢复、路线准备失败、响应不可信或增强 payload 超限时，系统锁存故障并阻断新的任务模型请求。

恢复由显式配置检查、服务重启或能力重新验证触发。系统不自动重放已中断的用户请求或工具操作。

### 4.7 用完整任务结果判断价值

质量以复杂长任务有效完成、目标与约束连续性、当前事实正确性和必要证据可恢复性判断。成本包含任务模型、记忆模型、召回、重试和故障处理产生的全部 API 请求。

压缩率、召回率、缓存命中率和延迟只解释机制，不替代可靠性、质量和增强独占。

### 4.8 关键结论由实际链路闭合

关键节点按其真实责任边界验证：本地数据结构使用真实文件和 Pi session，宿主集成使用真实 Pi 生命周期，记忆能力使用受管 OpenViking 与已配置的实际记忆 Provider/模型，任务结果和请求采用使用当前实际任务 Provider/模型，成本使用 Provider usage 或账单。替身只证明受控分支和故障语义，不能把模拟成功提升为系统完成证据。

验证控制平面在 suite manifest 中冻结任务与记忆的 Provider、模型、API、样本和停止条件。坐标不变时，实际付费调用是正常验证动作；只有改变任一任务或记忆 Provider/模型才属于需要用户决定的战略变更。只有具备 actual 证据的 Provider/模型/API 组合进入支持矩阵，替身适配不扩展架构承诺。

## 5. 系统组成

```text
Pi
└── pi-context-memory 扩展
    ├── Pi 集成
    ├── Session 记忆协调
    ├── 长时记忆 ─────────┐
    ├── 工作上下文优化 ───┼→ OpenViking
    └── 召回与来源追溯 ───┘

项目启动器
└── OpenViking 进程与记忆模型能力生命周期

开发验证
└── 质量与成本观测
    └── 验证复杂长任务可靠性、质量、增强独占与完整成本
```

扩展内模块共同承担运行责任。边界用于稳定职责与依赖方向，不要求每个模块对应独立进程或部署单元。

## 6. 模块战略目标与职责边界

### 6.1 Pi 集成模块

**架构定位**

系统与 Pi 之间的唯一集成边界，使其它模块无需依赖 Pi 生命周期、消息形态和 Provider hook 的具体实现。

**核心目标**

在保留 Pi Agent、会话、工具和交互能力的同时，确保扩展启用后的任务模型请求只使用增强记忆构造的工作上下文。

**职责**

- 感知 Pi session、branch、消息、工具、compaction、tree 和模型调用生命周期；
- 将当前会话事实传递给 Session 记忆协调模块；
- 在 `context` 边界等待并应用当前精确路线的增强上下文；
- 在最终 Provider 边界核验增强证明，并阻断不满足条件的请求；
- 取消 Pi compaction，并抑制 tree 的原生与扩展 branch summary；
- 向任务模型提供统一召回入口；
- 展示初始化、正常运行和故障状态；
- 提供记忆模型配置检查、受管实例重启和故障诊断入口。

**责任分工**

- Pi 负责 Agent 循环、会话、工具、任务模型和用户交互；
- 本模块把 Pi 事件转换为下游稳定输入，并执行最终请求授权；
- 本模块不生成 Working Memory、不选择记忆内容，也不管理 OpenViking 子进程。

### 6.2 Session 记忆协调模块

**架构定位**

每个 session 内记忆生命周期、运行状态和请求采用条件的协调者。

**核心目标**

确保正确 session、正确 branch、正确运行代际和正确内容在准备完成后进入任务模型输入，并使任何必要能力失败形成确定的阻断状态。

**职责**

- 维护 session、branch、来源和运行代际约束；
- 计算包含完整内容的精确路线身份；
- 协调来源归档、Working Memory、当前回合投影和工作上下文准备；
- 管理初始化、ready、故障和停止生命周期；
- 区分内部路线的准备、就绪和失败状态；
- 对并发、超时、迟到结果和取消进行有界治理；
- 向 Pi 集成提供允许请求或阻断请求的唯一决定。

**责任分工**

- Pi 集成执行 Provider 边界动作；
- 长时记忆和工作上下文优化执行具体数据处理；
- 协调状态表达内容有效性、路线归属和请求条件，不解析任务业务语义。

### 6.3 长时记忆模块

**架构定位**

当前任务模型输入之外的 session 信息承载层，也是 OpenViking 运行代际和记忆模型能力事实的提供者。

**核心目标**

让退出工作上下文的信息持续可用、可检索、可恢复，并让任务模型请求只依赖实际通过能力验证的记忆运行实例。

**职责**

- 保存 session 范围内的来源副本和完整工具结果；
- 保留派生信息与 Pi entry 的来源联系；
- 按当前路线维护隔离的 OpenViking Session 和 Working Memory；
- 验证 OpenViking 服务、配置和记忆模型实际能力；
- 发布受管运行代际及能力证明；
- 向工作上下文优化提供来源核验的 Working Memory 与活跃历史；
- 向召回模块提供可索引历史和来源。

**责任分工**

- Pi session 历史承担事实权威；
- Session 记忆协调决定结果何时可进入请求；
- OpenViking 提供派生记忆、索引和 context assembly；
- 本模块吸收 OpenViking 协议、配置和能力变化。

### 6.4 工作上下文优化模块

**架构定位**

任务模型实际工作上下文的构造者，也是自动压缩和上下文经济性的主要责任所有者。

**核心目标**

在保持任务可靠完成、工具协议完整、事实可信和来源可恢复的前提下，将跨轮历史与当前回合共同维持在有界稳定规模。

**职责**

- 组合当前有效 Working Memory、来源核验活跃历史和当前用户 prompt；
- 将 assistant 工具调用与对应结果组织为原子批次；
- 根据预算保留原批次或生成来源可恢复的有界投影；
- 保留状态、错误、否定结果、关键内容和原始来源入口；
- 为召回结果提供受控位置；
- 生成可在最终 Provider payload 核验的增强证明；
- 对空结果、未知内容、协议不完整和预算超限返回明确失败。

**责任分工**

- Session 记忆协调确认路线和输入有效性；
- 长时记忆保证来源与 Working Memory 可用；
- Pi 集成应用并核验最终消息；
- 本模块不修改 Pi session 历史，不决定当前 branch。

### 6.5 召回与来源追溯模块

**架构定位**

任务模型访问当前工作上下文之外信息的统一入口。

**核心目标**

在当前 session 和 branch 范围内找回早期信息，并将关键事实展开到可核验 Pi 来源。

**职责**

- 查询当前 session 和 branch 的有效历史；
- 返回 OpenViking 排序候选、来源副本预览和 Pi 权威 entry；
- 支持从派生线索展开到来源；
- 将召回结果以有限范围交给工作上下文优化模块。

**责任分工**

- OpenViking 提供候选和排序；
- Session 记忆协调限定当前有效范围；
- Pi session 来源核验事实；
- 本模块不管理记忆生命周期。

### 6.6 质量与成本观测模块

**架构定位**

产品价值和架构方向的开发验证边界。

**核心目标**

证明增强路径在必要外部服务可用时可靠完成真实复杂长任务，并在质量和增强独占成立后判断完整成本优势。

**职责**

- 定义复杂长任务、重复次数、checker 和外部阻塞证据；
- 归集 eligible、blocked、completed 和 failed 运行；
- 观察 Provider 请求增强证明、阻断原因、Pi 摘要调用与 branch summary entry 增量；
- 验证当前回合大输出、路线变化和长时间运行；
- 归集任务模型、记忆生成、召回、重试和故障处理的全部 API 成本；
- 比较增强路径与 Pi 原生基线的任务结果和完整账单。

**责任分工**

- 本模块运行于开发实验环境，不进入扩展运行；
- 任务 checker 决定有效完成；
- 独立服务观测决定 blocked 归因；
- 可靠性、质量和增强独占先于成本结论。

## 7. 模块协同关系

### 7.1 正常请求

```text
Pi 产生当前 session、branch 和消息事实
  → Pi 集成规范化路线与当前回合
  → Session 记忆协调取得精确路线身份和运行代际
  → 长时记忆确认来源、OpenViking 和记忆模型能力
  → 工作上下文优化构造有界增强历史与工具批次投影
  → Session 记忆协调确认路线、代际和内容仍然有效
  → Pi 集成在 context hook 应用增强消息
  → Pi 集成在 Provider 边界核验增强证明
  → Pi 发送任务模型请求
```

内部路线准备可以等待、排队和复用；用户状态持续显示“增强记忆”。

### 7.2 自动压缩与路线变化

```text
上下文增长或当前回合工具结果增加
  → 工作上下文优化按预算更新历史和工具批次投影
  → 长时记忆保存退出工作上下文的来源并更新 Working Memory
  → Pi session 权威历史保持不变

Pi compaction 事件
  → Pi 集成确定性取消；threshold/overflow 锁存故障，manual 给出提示

Pi tree 事件
  → Pi 集成抑制原生与扩展 summary，不新建 branch summary entry
  → 无摘要导航后的 leaf 成为唯一有效路线
  → 已有 summary entry 只保留无文本的路线边界身份
```

### 7.3 故障与恢复

```text
必要配置、服务、模型、来源、路线或内容条件失败
  → Session 记忆协调锁存故障及证据
  → Pi 集成在 Provider 前终止请求
  → 状态显示“增强记忆 · 故障”并提供诊断
  → 用户修复并显式重启或重新验证
  → 新运行代际完成能力验证
  → 状态恢复为“增强记忆”
  → 用户重新提交任务
```

系统不自动重放中断请求。当前 Pi session、branch 和来源继续作为恢复后的权威输入。

## 8. 架构评价

架构是否有效由以下结果判断：

1. 必要外部服务可用时，声明范围内的复杂长任务全部有效完成；
2. 每个已发送任务模型请求都具有当前代际、精确路线和增强内容证明；
3. 不满足条件的请求在 Provider 前被阻断，扩展运行期间不产生 Pi 原生上下文或摘要接续；
4. 多个快速、并行和大输出工具批次保持协议完整、输入有界和来源可恢复；
5. session 与 branch 身份能够隔离冲突路线和迟到结果；
6. 故障具有独立证据、稳定诊断和显式恢复路径；
7. 召回能够把关键事实展开到当前 Pi 来源；
8. 可靠性和质量成立后，完整 API 成本低于可比较的 Pi 原生基线。

模块设计、实现和验证必须说明其对这些结果的贡献。没有当前责任、消费者或验证路径的结构不进入系统。
