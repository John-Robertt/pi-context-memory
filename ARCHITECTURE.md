# pi-context-memory 系统架构

## 1. 文档角色与权威边界

本文是 `pi-context-memory` 的系统架构权威文档，定义系统及各模块为到达产品目的地所承担的架构定位、核心目标、职责边界和协作关系。

`PRODUCT.md` 是产品目标、用户需求、产品范围和价值评价的权威来源。本文承接产品文档定义系统路线，不重新定义或改变产品价值。

本文不规定具体的数据结构、接口签名、存储格式、阈值、重试次数、部署方式或实现步骤；这些属于设计与实现决策，但不得改变本文定义的战略目标和职责边界。

具体功能、系统、模块、契约、验证与运行文档统一存放在 [`docs/`](docs/README.md)，其归属和生命周期由 [`DOCUMENTION.md`](DOCUMENTION.md) 约束。这些文档服从本文，只负责在既定架构边界内定义战术设计、交付顺序和验证方式。

本文只维护当前有效的目标架构，不记录实施进度、候选技术路线、实验过程或已被替代的设计。实现与验证可以为架构调整提供证据，但不能成为与本文竞争的系统权威来源。

## 2. 整体战略目标

为实现 `PRODUCT.md` 定义的产品目的地，`pi-context-memory` 的系统战略目标是：

> **在不替代 Pi、不改变 Pi 核心使用方式的前提下，为每个 Pi session 提供自动、隔离、可追溯的长时记忆，并在任务质量不劣于 Pi 原生路径的前提下降低长会话的完整 API 成本，从而使固定预算能够支持更长、更连续的有效任务推进。**

这个目标落实为三个相互依赖的架构结果；结果之间的取舍遵循 `PRODUCT.md` 定义的产品价值优先级：

1. **任务连续性**：长会话经过多次工作上下文换代后，目标、约束、当前有效事实和关键证据仍能支持任务继续推进。
2. **完整成本优势**：在达到同等任务质量的前提下，系统触发的全部 API 请求总成本低于 Pi 原生长会话路径。
3. **透明增强**：用户继续使用 Pi，记忆维护与成本优化由扩展自动完成，不把记忆管理负担转移给用户或任务模型。

任务质量是成本优化的前提。发生冲突时，系统应先保持任务可继续和事实可恢复，允许暂时失去记忆增强或成本优势。

本文的模块、原则和边界服务于整体战略目标。实践结果用于校准设计，并优先采用贡献明确、结构更小且更可靠的实现。

## 3. 产品与系统定位

### 3.1 产品定位

产品定位以 [`PRODUCT.md`](PRODUCT.md) 为准。系统附着于 Pi 的扩展生命周期，在 Pi 原有会话、模型调用和交互体验之上提供增强能力。Pi 继续负责 Agent 循环、会话交互、工具执行、模型接入和原生上下文能力；本项目只负责与长时记忆和上下文经济性直接相关的能力。

### 3.2 最高隔离边界

一个 Pi session 对应一个独立长任务，也是记忆的最高隔离边界。不同 session 之间不自动共享：

- 长时记忆；
- 工作上下文；
- 召回结果；
- 任务状态；
- 成本归属。

同一 session 内，当前 branch 表示从会话根到当前活动位置的事实与决定有效路线。Pi 的对话回退、树导航和分支操作以新的当前 leaf 重新定义有效路线；工作上下文只采用属于该路线且完成当前路线核验的派生记忆。

### 3.3 事实与派生信息边界

Pi 原生 session 历史是会话事件身份、顺序和分支关系的权威来源。本系统保存的长期归档是用于检索和恢复的事实副本；工作记忆、摘要和索引均是可重建的派生信息。

派生信息可以提升工作效率，但不能覆盖权威历史，也不能成为关键事实不可恢复的唯一载体。

目标、验收条件和硬约束构成任务连续性的稳定锚点。锚点及其变更必须能够回到当前有效路线中的用户来源；工作记忆可以投影这些内容，但不能仅凭有损摘要改写它们。

### 3.4 管理责任边界

记忆何时更新、工作上下文何时换代、哪些结果可以进入当前上下文以及故障时如何降级，均由系统负责。

任务模型负责完成用户任务和发起必要召回；系统负责记忆的压缩、提交、合并、删除和修复。扩展运行期间，系统还负责向用户持续呈现当前模型输入实际采用的增强或 Pi 原生记忆与压缩路径，运行状态以实际采用结果为准。

## 4. 架构原则

### 4.1 通过 Pi 扩展提供增强

系统通过 Pi 扩展提供能力，不重新实现 Agent Runtime、会话管理器、独立 CLI、进程监督器或通用任务调度平台。只有经实践证明 Pi 扩展边界无法满足本项目战略目标时，才重新评估宿主边界。

### 4.2 记忆服务于任务，不成为新的事实权威

长期记忆让退出当前模型上下文的信息继续可用。派生记忆服从当前 session、当前 branch 和原始来源。

### 4.3 以稳定工作上下文获得成本优势

系统维持足够小、内容稳定、可持续追加的工作上下文，并在必要时进行受控换代。Provider 缓存放大成本收益；任务正确性由上下文内容和来源保证。

### 4.4 优化故障由 Pi 原生路径接续

记忆后端故障或恢复、记忆更新超时、派生结果待核验时，系统保持当前可信上下文或采用 Pi 原生路径，使用户持续推进任务。

系统采用与产品风险相称的可靠性。异常治理限制影响、阻止错误记忆生效并保持 Pi 可用；低频后端故障使用现有扩展边界处理。

### 4.5 用完整结果判断价值

成本包含本系统为一次任务触发的全部 API 请求。质量以任务完成、目标与约束连续性、当前事实正确性和必要证据可恢复性判断；压缩率、召回率和缓存命中率用于解释结果。

## 5. 系统组成

```text
Pi
└── pi-context-memory 扩展
    ├── Pi 集成
    ├── Session 记忆协调
    ├── 长时记忆
    ├── 工作上下文优化
    └── 召回与来源追溯
             │
             └── 可替换的记忆后端

开发验证
└── 质量与成本观测
    └── 比较 Pi 原生与增强路径，不进入扩展运行
```

扩展内模块共同承担运行责任，边界用于稳定依赖方向，不要求每个模块对应独立进程、服务或部署单元。质量与成本观测只属于开发验证，用于检验产品目标，不随扩展运行，也不向用户提供账单能力。

## 6. 模块战略目标与职责边界

### 6.1 Pi 集成模块

**架构定位**

系统与 Pi 之间的唯一集成边界，使其它模块无需依赖 Pi 生命周期和扩展机制的具体形态。

**核心目标**

在保留 Pi 原生使用方式和控制权的同时，为记忆与上下文优化提供必要的会话观察、模型上下文介入和用户能力入口。

**职责**

- 感知与记忆相关的 Pi session、branch、对话回退、树导航、消息、压缩和模型调用生命周期；
- 将 Pi 当前会话状态传递给 Session 记忆协调模块；
- 将工作上下文优化结果应用于模型输入；
- 向任务模型提供统一召回能力；
- 增强准备、故障和恢复期间由 Pi 原生执行、记忆与压缩路径接续；
- 依据每次模型输入实际采用的路径，向用户展示增强或 Pi 原生状态。

**责任分工**

- Pi 负责 Agent 循环、会话、工具、模型和用户交互；
- 本模块把 Pi 内部事件转换为当前记忆系统所需的稳定输入；
- 故障隔离保持 Pi 原生执行路径可用。

**协作关系**

它向 Session 记忆协调模块提供会话事实，接收工作上下文优化模块的投影结果，并承载召回与来源追溯模块面向任务模型的入口。

### 6.2 Session 记忆协调模块

**架构定位**

每个 session 内记忆生命周期的协调者，负责让长期记忆、当前 branch 和模型工作上下文保持一致。

**核心目标**

确保正确 session、正确 branch、正确阶段的记忆在可信时进入工作上下文，并在异步更新、迟到结果或故障发生时保持当前有效上下文不被错误替换。

**职责**

- 维护 session、branch 和任务锚点对记忆有效性的约束；
- 协调历史进入长时记忆以及新工作记忆进入上下文的时机，以 Pi 当前 leaf 建立每次路线的采用边界；
- 区分已从权威历史读取、已可靠保存、派生完成和可在当前路线采用等状态，不以后台完成状态代替内容有效性；
- 对超时、失败和迟到结果进行有界治理；
- 决定继续使用当前记忆、采用新记忆或回到 Pi 原生路径。

**责任分工**

- 任务模型执行用户任务并理解业务语义；
- 协调状态表示内容有效性、路线归属与采用条件；
- 现有有界队列和至少一次重试语义承担异步治理。

**协作关系**

它接收 Pi 集成模块提供的会话变化，驱动长时记忆模块更新派生记忆，并只把当前有效结果交给工作上下文优化模块。

### 6.3 长时记忆模块

**架构定位**

当前模型工作上下文之外的 session 级信息承载层，统一隔离具体记忆后端。

**核心目标**

让退出当前工作上下文的任务信息仍可被持续利用和按来源恢复，同时使记忆后端能够在不改变系统其它职责的情况下替换或演进。

**职责**

- 保存 session 范围内可检索的历史副本和需要退出工作上下文的完整工具结果；
- 维护用于延续任务的累计工作记忆；
- 保持所有数据和查询严格属于目标 session；
- 保留派生信息与原始来源之间的联系；
- 向协调、投影和召回模块提供统一的记忆能力。

**责任分工**

- Pi session 历史承担事实权威；
- Session 记忆协调模块决定记忆采用时机；
- 本模块保存可重建派生信息及其来源关系；
- 当前产品范围以 session 隔离，上层通过统一记忆能力访问后端。

**协作关系**

它由 Session 记忆协调模块驱动更新，为工作上下文优化模块提供当前有效工作记忆，并为召回与来源追溯模块提供历史内容和来源。


### 6.4 工作上下文优化模块

**架构定位**

任务模型实际工作上下文的构造者，也是成本优势的主要实现责任所有者。

**核心目标**

在保留任务目标、当前约束、有效事实和必要证据入口的前提下，将模型输入维持在有界且稳定的规模，并减少长历史在后续请求中的重复成本。

**职责**

- 组合任务锚点、当前有效工作记忆与新增会话内容；
- 对不适合长期留在工作上下文中的大工具结果使用稳定、有界的投影，并保留状态、错误、否定结果和原始来源等后续行动所需信息；
- 保持一段工作周期内既有上下文稳定，并以追加方式容纳新内容；
- 在上下文需要换代时使用已经确认有效的新工作记忆；
- 为召回结果提供进入当前工作上下文的受控位置；
- 在无法保证投影正确时放弃优化并使用 Pi 原生上下文能力。

**责任分工**

- Pi session 历史保持完整，长期记忆模块生成记忆；
- 工作上下文优化以任务质量为首要条件；
- Provider 缓存作为收益放大机制；
- 稳定追加与有条件换代控制上下文重写频率。

**协作关系**

它只接收 Session 记忆协调模块确认有效的工作记忆，从 Pi 集成模块获得当前新增会话内容，并将最终工作上下文交回 Pi 集成模块。

### 6.5 召回与来源追溯模块

**架构定位**

任务模型访问当前工作上下文之外信息的唯一记忆入口。

**核心目标**

在不暴露记忆管理复杂度的情况下，使任务模型能够按需找回早期信息，并将关键事实追溯到可核验的 session 来源。

**职责**

- 在当前 session 和当前 branch 的有效范围内查询历史；
- 统一返回工作记忆、历史副本和原始来源中的相关信息；
- 支持从派生线索展开到可核验来源；
- 将召回结果以明确来源和有限范围交给工作上下文优化模块。

**责任分工**

- Session 记忆协调模块控制当前有效记忆；
- Pi session 来源核验事实，相似度与摘要提供线索；
- 查询范围限定为当前 session 和当前 branch；
- 记忆生命周期由系统自动管理，入口聚焦任务召回。

**协作关系**

它从长时记忆模块获取线索和来源，在必要时关联 Pi 原生 session 历史，并通过工作上下文优化模块把结果加入当前模型输入。

### 6.6 质量与成本观测模块

**架构定位**

产品价值和架构方向的开发验证边界，负责回答“是否保持质量”和“是否真正降低完整成本”。该模块运行于开发实验环境。

**核心目标**

以可比较的任务结果和完整成本证据持续验证系统是否正在接近整体战略目标，并识别记忆或优化策略造成的退化。

**职责**

- 在固定开发实验中归集任务模型、记忆生成、召回、重试和降级实际产生的 API 成本；
- 观察任务完成、记忆连续性、当前事实正确性和来源恢复结果；
- 区分正常增强路径与降级路径的结果；
- 向架构和策略调整提供可比较的事实依据。

**责任分工**

- Pi session 历史承担事件事实，工作上下文模块承担运行决策；
- 任务结果与完整 API 成本决定实验结论，辅助指标解释机制；
- Provider、Pi 和记忆后端保持各自运行控制权；
- 本模块在开发验证进程中读取专用实验凭据并保存脱敏结果。

**协作关系**

它从固定开发实验接收与质量和成本有关的结果，独立于任务执行和产品运行；其结论用于检验整体战略和各模块策略，不形成用户侧状态或能力。

## 7. 模块协同关系

正常协作遵循以下方向：

```text
Pi 产生会话事实
  → Pi 集成模块识别当前 session 与 branch
  → Session 记忆协调模块判断记忆生命周期
  → 长时记忆模块保存历史副本并形成工作记忆
  → Session 记忆协调模块确认当前可用记忆
  → 工作上下文优化模块构造稳定、有界的模型输入
  → Pi 集成模块应用模型输入并展示实际采用路径
  → Pi 继续执行任务

模型需要早期信息
  → 召回与来源追溯模块在当前 session 范围内查询
  → 工作上下文优化模块将结果加入当前工作上下文
  → 模型依据来源继续任务

各模块产生质量与成本结果
  → 质量与成本观测模块汇总
  → 验证系统是否达到整体战略目标
```

故障协作遵循以下方向：

```text
记忆更新失败、超时或结果不可信
  → Session 记忆协调模块不采用该结果
  → 工作上下文优化模块保留当前有效上下文
     或放弃增强并回到 Pi 原生记忆与压缩路径
  → Pi 集成模块显示当前采用 Pi 原生路径
  → Pi 继续执行任务
  → 质量与成本观测模块记录实际影响
```

## 8. 架构评价

产品价值如何评价只由 `PRODUCT.md` 的“产品价值评价”定义。本文不重复建立系统成功条件，只评价当前职责划分和协作关系是否有效支撑那些产品结果：

1. Pi 集成边界使扩展可以增强或退出，而不接管 Pi 的核心能力；
2. Session 记忆协调依据 session、当前 branch 和任务锚点控制记忆生效，工作上下文只采用完成当前路线核验的结果；
3. 长时记忆在后端可替换的同时保留完整来源，使派生信息可恢复、可核对；
4. 工作上下文保持有界、稳定和可持续追加，并在不损害任务质量的前提下减少历史重复成本；
5. 召回为上下文外信息提供统一且受当前任务边界约束的来源恢复路径；
6. 质量与成本观测能够用实际任务结果和完整 API 成本解释正常、退化与失败路径。

这些结果用于持续判断架构是否把产品推向战略目标，不构成彼此独立的固定硬门槛。模块设计、技术选型和实现应说明预期贡献，并用实践结果检验；不能继续贡献目标，或已有更小结构承担同一责任的内容，应被校准、替换或移除。
