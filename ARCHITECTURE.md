# pi-context-memory 系统架构

## 1. 文档角色与权威边界

本文是 `pi-context-memory` 的系统架构权威文档，定义系统及各模块为到达产品目的地所承担的架构定位、核心目标、职责边界和协作关系。

[`PRODUCT.md`](PRODUCT.md) 是产品目标、用户需求、产品范围和价值评价的权威来源。本文承接产品文档定义系统路线，不重新定义产品价值。

本文不规定具体的数据结构、接口签名、存储格式、阈值、重试次数、部署方式或实现步骤。具体功能、系统、模块、契约、验证与运行文档统一存放在 [`docs/`](docs/README.md)，其归属和生命周期由 [`DOCUMENTION.md`](DOCUMENTION.md) 约束。

本文只维护当前有效的目标架构。实现进度和证据状态由 [`docs/DEVELOPMENT.md`](docs/DEVELOPMENT.md) 维护。

## 2. 整体战略目标

为实现 `PRODUCT.md` 定义的产品目的地，系统战略目标是：

> **在不替代 Pi Agent、会话、工具、模型、扩展调度和交互能力的前提下，为持久化 Pi session 提供自动、隔离、可追溯且可靠的长时记忆；本扩展只确认自己构造的增强上下文，准确观测宿主边界，并在任务质量成立后降低完整 API 成本。**

这个目标落实为五个相互依赖的架构结果：

1. **可靠任务执行**：在声明支持的环境中，内部等待、并发、输出规模、上下文换代和路线变化不造成任务中断。
2. **事实与路线可信**：本扩展的来源、记忆和增强输出只采用当前 session、branch 与可核验来源。
3. **增强采用可证**：本扩展构造的上下文和自身 hook 时点具有证明，实际 Provider 采用由职责外 transport 观测确认。
4. **有界工作上下文**：跨轮历史和当前回合工具结果共同维持在稳定预算内，并保持来源可恢复和 Provider 协议完整。
5. **完整成本优势**：可靠性和任务质量成立后，系统触发的全部 API 请求总成本低于可比较的 Pi 原生长会话基线。

任务可靠完成和事实可信是增强采用与成本优化的前提。实现不能通过放宽路线、丢弃必要来源或越权控制 Pi/其它扩展来掩盖失败。

## 3. 产品与系统定位

### 3.1 Pi 宿主边界

系统附着于 Pi 扩展生命周期。Pi 继续负责 Agent 循环、会话持久化、工具执行、模型接入、扩展调度、Provider transport 和用户交互；本系统只负责自己持有的记忆、工作上下文、召回、运行状态，以及在公开 hook 中表达的处理结果。

Pi session 历史、tree、branch 和用户操作语义保持权威。扩展可以构造替代上下文、拒绝确认自身无法证明的增强请求，并观测自身 handler 时点；它不控制 Pi、其它扩展、加载顺序或后续 transport。超出边界的问题只记录事实、时点与不确定性，由用户决定处理方式。

本扩展不在运行期把内部失败伪装为增强成功。继续当前 Pi/扩展组合、修复条件、禁用扩展或重启 Pi 属于用户决定，不由扩展自动执行。

### 3.2 最高隔离边界

一个 Pi session 对应一个独立长任务，也是记忆的最高隔离边界。不同 session 之间不自动共享：

- 长时记忆；
- 工作上下文；
- 召回结果；
- 任务状态；
- 故障状态；
- 成本归属。

同一 session 内，当前 branch 表示从会话根到当前 leaf 的事实与决定有效路线。对话回退、树导航、fork、clone、resume 和 reload 以操作后的当前 leaf 重新定义唯一有效路线。

增强记忆只处理具有权威 session file 的持久化 Pi session。缺少稳定文件时，本扩展不能建立可恢复身份，不确认增强输出并调用 abort；实际 transport 结果另行观测。

### 3.3 事实与派生信息边界

Pi session 历史是会话事件身份、顺序和分支关系的权威来源。本系统保存的来源归档是用于恢复和检索的事实副本；Working Memory、OpenViking abstract、索引、工具批次投影和工作上下文均是可重建的派生信息。

派生信息必须绑定当前 session、当前 branch、运行代际和来源。它可以替代历史内容进入任务模型输入，但不能覆盖 Pi 权威历史，也不能成为关键事实不可恢复的唯一载体。

目标、验收条件和硬约束构成任务连续性的稳定锚点。锚点及其变更必须能够回到当前有效路线中的用户来源。

### 3.4 管理责任边界

系统负责：

- 记忆模型能力何时达到可运行条件；
- 来源何时归档和核验；
- 工作上下文何时更新和换代；
- 当前回合工具结果如何保持有界；
- 本扩展在自己的请求 hook 时点确认哪些增强输出；
- 本扩展通过公开 hook 请求怎样处理 compaction 与 tree summary，并如何记录实际可观测结果；
- 故障如何锁存、诊断和恢复。

任务模型负责完成用户任务和按需发起来源召回，不管理记忆生命周期。

用户状态只呈现“增强记忆 · 初始化中”“增强记忆”和“增强记忆 · 故障”。后台记忆刷新、必要等待、队列和镜像生命周期属于内部观测，不形成用户状态。

## 4. 架构原则

### 4.1 通过 Pi 扩展提供单一增强路径

Pi 是宿主与控制平面，OpenViking 是记忆与上下文能力提供者，扩展承担双方接口、数据和版本变化的适配成本。项目启动器只管理自己创建的 OpenViking 实例并以启动标识确认所有权。

扩展运行实例从初始化开始只构造增强路径。配置、服务、模型能力、路线或内容核验未满足时，本扩展拒绝确认自己的增强输出并给出诊断；是否继续或调整宿主组合由用户决定。

### 4.2 增强输出以本扩展证明为前置条件

本扩展发布的每个增强输出必须绑定：

- 当前受管 OpenViking 运行代际；
- 当前 Pi session 与 session file；
- 当前 leaf 和有序 branch 来源；
- 完整路线指纹；
- 本扩展构造的工作上下文内容证明。

`context` 阶段负责等待、构造并核验这些扩展自有事实。`before_provider_request` 只以协议无关标记观察本扩展 handler 时点是否仍能识别该次增强内容；观察缺失、歧义或变化只形成兼容性诊断，不限制任务 Provider API、不锁存记忆故障，也不阻断宿主请求。时点后是否被 Pi 或其它扩展改变不属于本扩展可控制事实，最终采用另由 transport 观测；无法观测时不得提升为成功结论。

### 4.3 记忆服务于任务，不成为新的事实权威

长期记忆让退出当前模型输入的信息继续可用。来源、路线与用户决定仍由 Pi session 定义；相似度、摘要和 Working Memory 只提供派生线索和有界投影。

### 4.4 跨轮历史与当前回合共同受预算约束

工作上下文由与当前路线前缀一致的已核验派生记忆、来源可恢复的新近路线后缀、当前用户 prompt 和当前回合工具批次共同组成。后台记忆刷新只更新派生记忆，不改变事实来源或请求路径；现有派生记忆与新近来源仍能满足可信度和预算时，任务不等待刷新。目标、约束和有效决定依靠派生记忆与用户来源连续性保持，不建立第二套语义提取状态。

assistant 工具调用与结果按不可拆协议单元治理。缩小时只投影本扩展能够保持 Pi 语义的完整批次；Pi 基线可见但无记忆表示的内容原样 opaque 保留，必须替换却无法表示时才报告本扩展能力边界。

### 4.5 增强记忆只拥有自己的上下文换代

本扩展构造的模型历史只使用增强记忆。它在公开 hook 中返回取消 compaction 和无摘要 tree 的处理意见，但这些返回值不构成对 Pi 或其它扩展行为的要求；实际是否成立由宿主结果观测。

Pi session 保留完整权威历史。已有 compaction/branch summary 只参与本扩展路线解释，其文本不进入本扩展来源、VLM、索引或增强历史；Pi 或其它扩展随后如何使用这些 entry 不由本扩展控制。

### 4.6 故障阻断保护任务可信度

配置无效、必要服务不可用、记忆模型能力失败、来源不可恢复、必要刷新失败、响应不可信或增强 payload 超限时，本扩展锁存故障并拒绝确认新的增强输出。

用户选择配置检查、服务重启或能力重新验证后才建立恢复代际；本扩展不自动重放已中断的 prompt 或工具操作。

### 4.7 用完整任务结果判断价值

质量以复杂长任务有效完成、目标与约束连续性、当前事实正确性和必要证据可恢复性判断。成本包含任务模型、记忆模型、召回、重试和故障处理产生的全部 API 请求。

压缩率、召回率、缓存命中率和延迟只解释机制，不替代可靠性、质量和增强采用证据。

### 4.8 关键结论由实际链路闭合

关键节点按其真实责任边界验证：本地数据结构使用真实文件和 Pi session，宿主集成使用真实 Pi 生命周期，记忆能力使用受管 OpenViking 与已配置的实际记忆 Provider/模型，任务结果和请求采用使用当前实际任务 Provider/模型，成本使用 Provider usage 或账单。替身只证明受控分支和故障语义，不能把模拟成功提升为系统完成证据。

验证 manifest 固定当前 evidence 的 Provider、模型、API、Pi、OpenViking 与 extension composition；坐标或扩展组合变化需要用户决定并重新验证，但不形成产品运行白名单。记忆模型配置面只接受产品拥有的最小字段，并要求这些字段存在于项目锁定 OpenViking 的权威 VLM schema；Provider 和模型是否可用只由受管进程的实际能力探针决定。扩展加载顺序保持用户控制。

## 5. 系统组成

```text
Pi
└── pi-context-memory 扩展
    ├── Pi 集成
    ├── Session 记忆协调
    ├── 长时记忆 ─────────┐
    ├── 工作上下文优化 ───┼→ OpenViking
    └── 召回与来源追溯 ───┘

项目启动器
└── OpenViking 进程与记忆模型能力生命周期

开发验证
└── 质量与成本观测
    └── 验证复杂长任务可靠性、质量、增强采用、责任边界与完整成本
```

扩展内模块共同承担运行责任。边界用于稳定职责与依赖方向，不要求每个模块对应独立进程或部署单元。

## 6. 模块战略目标与职责边界

### 6.1 Pi 集成模块

**架构定位**

系统与 Pi 之间的唯一集成边界，使其它模块无需依赖 Pi 生命周期、消息形态和 Provider hook 的具体实现。

**核心目标**

在保留 Pi Agent、会话、工具和交互能力的同时，为本扩展管理的记忆与工作上下文提供准确、可核验且不越权的集成结果。

**职责**

- 感知 Pi session、branch、消息、工具、compaction、tree 和模型调用生命周期；
- 依据 Pi 公开转换契约建立 Provider 基线，并向下游发布具有结构证据的记忆投影；
- 在 `context` 边界等待并应用当前精确路线的增强上下文，同时保留未被增强覆盖的 Pi Provider 内容；
- 在自身 `before_provider_request` handler 时点以协议无关标记观察增强内容，异常只形成兼容性诊断，最终 transport 采用只作外部观测结论；
- 通过公开 hook 请求取消 Pi compaction 和无摘要 tree，并记录实际宿主结果；
- 向任务模型提供统一召回入口；
- 展示初始化、正常运行和故障状态，并把 Pi 的任务模型上下文用量标识为增强路径观测；
- 提供记忆模型配置检查、受管实例重启和故障诊断入口。

**责任分工**

- Pi 负责 Agent 循环、会话、工具、扩展调度、任务模型、Provider transport 和用户交互；
- 本模块把 Pi 事件转换为下游稳定输入，只确认本扩展在可观察 hook 中产生的结果；
- 本模块不生成 Working Memory、不选择记忆内容、不管理 OpenViking 子进程，也不控制其它扩展或后续 payload。

### 6.2 Session 记忆协调模块

**架构定位**

每个 session 内记忆生命周期、运行状态和增强输出条件的协调者。

**核心目标**

确保本扩展只为正确 session、branch、运行代际和内容构造增强结果；已有可信历史可用时让后台刷新并行，确实依赖刷新时建立屏障，并把必要能力失败表达为本扩展确定的 block 结论。

**职责**

- 维护 session、branch、来源和运行代际约束；
- 计算包含完整内容的精确路线身份；
- 协调来源归档、派生记忆检查点、当前路线后缀、当前回合投影和工作上下文准备；
- 管理初始化、ready、故障和停止生命周期；
- 判断检查点与路线后缀能否支持增强构造，只为必要刷新建立等待屏障；
- 对并发、超时、迟到结果和取消进行有界治理；
- 向 Pi 集成提供本扩展 `allow(enhancedContext, proof) | block(fault)` 决定。

**责任分工**

- Pi 集成执行本扩展公开 hook 中可观察的动作；
- 长时记忆和工作上下文优化执行具体数据处理；
- 协调状态表达内容有效性、路线归属和请求条件，不解析任务业务语义。

### 6.3 长时记忆模块

**架构定位**

当前任务模型输入之外的 session 信息承载层，也是 OpenViking 运行代际和记忆模型能力事实的提供者。

**核心目标**

让退出工作上下文的信息持续可用、可检索、可恢复，并只向本扩展提供来自实际通过能力验证的记忆运行实例的派生结果。

**职责**

- 保存 session 范围内的来源副本和完整工具结果；
- 保留派生信息与 Pi entry 的来源联系；
- 按当前路线维护隔离的 OpenViking Session，并发布绑定精确路线前缀与来源的派生记忆检查点；
- 验证 OpenViking 服务、配置、内部运行 profile 和记忆模型实际能力；
- 发布受管运行代际及能力证明；
- 向工作上下文优化提供已核验检查点；
- 向召回模块提供可索引历史和来源。

**责任分工**

- Pi session 历史承担事实权威；
- Session 记忆协调决定结果何时可进入请求；
- OpenViking 提供派生记忆、索引和 context assembly；
- 本模块吸收 OpenViking 协议、配置和能力变化。

### 6.4 工作上下文优化模块

**架构定位**

本扩展增强工作上下文的构造者，也是自身预算与投影经济性的责任所有者。

**核心目标**

在保持任务可靠完成、工具协议完整、事实可信和来源可恢复的前提下，将跨轮历史与当前回合共同维持在有界稳定规模。

**职责**

- 组合当前可用的派生记忆检查点、来源可恢复的当前路线后缀和当前用户 prompt；
- 将 assistant 工具调用与对应结果组织为原子批次；
- 根据预算保留原批次或生成来源可恢复的有界投影；
- 按结构字段保留执行状态、isError/cancelled/truncated/stopReason、固定 head/tail、哈希和来源入口；
- 为召回结果提供受控位置；
- 生成绑定扩展自有运行、路线、来源、预算和内容事实的增强证明与协议无关观察标记；
- 对空结果、协议不完整和预算超限返回明确失败；对不透明 Provider 内容先原样保留，无法在预算内保持时报告自身表示能力边界。

**责任分工**

- Session 记忆协调确认路线和输入有效性；
- 长时记忆保证来源与 Working Memory 可用；
- Pi 集成应用构造结果并核验本扩展 handler 时点消息；最终 transport 另行观测；
- 本模块不修改 Pi session 历史，不决定当前 branch。

### 6.5 召回与来源追溯模块

**架构定位**

任务模型访问当前工作上下文之外信息的统一入口。

**核心目标**

在当前 session 和 branch 范围内找回早期信息，并将关键事实展开到可核验 Pi 来源。

**职责**

- 查询当前 session 和 branch 的有效历史；
- 返回 OpenViking 排序候选、来源副本预览和 Pi 权威 entry；
- 支持从派生线索展开到来源；
- 将召回结果以有限范围交给工作上下文优化模块。

**责任分工**

- OpenViking 提供候选和排序；
- Session 记忆协调限定当前有效范围；
- Pi session 来源核验事实；
- 本模块不管理记忆生命周期。

### 6.6 质量与成本观测模块

**架构定位**

产品价值和架构方向的开发验证边界。

**核心目标**

证明本扩展在必要外部服务可用时可靠支持真实复杂长任务，并在增强采用结论与责任边界准确后判断完整成本优势。

**职责**

- 定义复杂长任务、重复次数、checker 和外部阻塞证据；
- 归集 eligible、blocked、completed 和 failed 运行；
- 分别观察 Provider 基线、记忆投影、hook 时点证明、transport 最终采用、compaction/tree handler 返回与实际宿主结果；
- 验证当前回合大输出、路线变化和长时间运行；
- 归集任务模型、记忆生成、召回、重试和故障处理的全部 API 成本；
- 比较增强路径与 Pi 原生基线的任务结果和完整账单。

**责任分工**

- 本模块运行于开发实验环境，不进入扩展运行；
- 任务 checker 决定有效完成；
- 独立服务观测决定 blocked 归因；
- 可靠性、质量、增强采用证据和责任边界先于成本结论。

## 7. 模块协同关系

### 7.1 正常请求

```text
Pi 发布当前 session、branch 和 Provider 基线
  → Pi 集成提供结构化来源、控制边界与必须原样保留的 Provider 内容
  → Session 记忆协调绑定精确路线和运行代际，选择可采用的派生记忆与当前来源后缀
  → 长时记忆核验来源、运行能力和检查点，按需执行后台刷新
  → 工作上下文优化在任务模型预算内构造增强历史
  → Session 记忆协调重新确认路线、代际和内容
  → Pi 集成应用本扩展输出并记录自身 hook 结果
  → Pi 与其它扩展继续宿主生命周期，transport 独立观测最终采用
```

后台记忆刷新可以等待、排队、合并和复用；它不是路线可用性的同义词。只有现有检查点与新近来源无法形成可信、有界输入时，请求才等待对应刷新；用户状态持续显示“增强记忆”。

### 7.2 自动压缩与路线变化

```text
上下文增长或当前回合工具结果增加
  → 工作上下文优化按预算更新历史和工具批次投影
  → 长时记忆保存退出工作上下文的来源，并在后台把稳定路线前缀刷新为新的派生记忆检查点
  → Pi session 权威历史保持不变

Pi compaction/tree 事件
  → Pi 集成通过公开 hook 返回取消或无摘要处理意见
  → Pi 与其它扩展继续事件处理
  → 实际 Provider 请求和 session entry 形成宿主结果观测
  → 不符合时记录 host-behavior-unverified，不修改其它组件
  → Pi 实际操作后的 leaf 成为增强路线输入
  → 已有 summary entry 只保留无文本的路线边界身份
```

### 7.3 故障与恢复

```text
必要配置、服务、模型、来源、路线或内容条件失败
  → Session 记忆协调锁存故障及证据
  → Pi 集成停止自身构造/确认并调用 abort；transport 结果另行观测
  → 状态显示“增强记忆 · 故障”并说明本扩展责任阶段
  → 不修改 Pi、其它扩展、顺序或 Provider/模型
  → 用户决定继续当前组合、禁用扩展，或修复后请求重新验证
  → 仅重新验证分支创建新运行代际
  → 新运行代际完成能力验证
  → 状态恢复为“增强记忆”
  → 用户重新提交任务
```

系统不自动重放中断请求。当前 Pi session、branch 和来源继续作为恢复后的权威输入。

## 8. 架构评价

架构是否有效由以下结果判断：

1. 必要外部服务可用时，声明范围内的复杂长任务全部有效完成；
2. 每个构造输出绑定当前代际、精确路线、来源、预算与内容证明；hook 准确记录 observed/changed/missing/no-constructed-output，异常不阻断或锁存记忆运行；
3. transport 最终采用被独立分类，不把 hook observed 或无法观察误报为 Provider 采用；
4. Provider 基线遵循 Pi 转换；记忆投影仅用结构证据，Pi 可见 foreign/opaque 内容不因本扩展未知而删除；
5. 多个快速、并行和大输出工具批次保持协议完整、输入有界和来源可恢复；
6. session 与 branch 身份能够隔离冲突路线和迟到结果；
7. 故障诊断不越权修改 Pi、其它扩展或 Provider/模型；
8. 召回能够把关键事实展开到当前 Pi 来源；
9. 可靠性和质量成立后，完整 API 成本低于可比较的 Pi 原生基线。

模块设计、实现和验证必须说明其对这些结果的贡献。没有当前责任、消费者或验证路径的结构不进入系统。
