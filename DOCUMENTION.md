# pi-context-memory 文档架构

## 1. 文档角色与权威边界

本文是 `pi-context-memory` 的**文档架构权威文档**，定义项目文档体系为什么存在、应到达什么状态、各类文档承担什么长期责任、信息如何在文档之间流动，以及未来开发应依据什么原则创建、修改、拆分和删除文档。

本文与 [`PRODUCT.md`](PRODUCT.md) 和 [`ARCHITECTURE.md`](ARCHITECTURE.md) 同属战略文档，但三者的决策对象不同：

- `PRODUCT.md` 决定产品为谁存在、解决什么问题以及怎样判断价值；
- `ARCHITECTURE.md` 决定系统用哪些责任边界和协作关系到达产品目的地；
- `DOCUMENTION.md` 决定项目知识如何组织，确保产品、架构、设计、验证、运行和当前进度各有唯一、可持续的权威位置。

本文不规定当前应选择哪些接口、数据结构、算法、存储、测试框架、命令或部署方式，也不提前创建具体功能和模块设计。具体内容由未来开发在本文件定义的文档责任内形成。

[`docs/README.md`](docs/README.md) 是当前文档入口，只索引已经存在且仍然有效的文档；它服从本文，不能改变文档类别、权威边界或治理原则。

## 2. 文档体系的战略目标

项目文档体系的战略目标是：

> **让任何参与当前开发或维护的人，都能从产品目的地出发，沿唯一且连续的权威路径找到当前有效的功能、架构、设计、契约、验证、运行和开发状态；并让每次变化在进入实现前拥有明确归属、边界和验证方式，在实现成立后只保留与当前事实一致的最少文档。**

这个目标落实为五个相互依赖的结果：

1. **方向一致**：所有战术文档都能回到产品目标和架构责任，不在局部文档中改变战略；
2. **事实唯一**：每项当前有效事实只有一个权威位置，其它位置只引用，不复制出竞争版本；
3. **设计先行**：行为、数据、模块边界或跨模块协作发生变化前，先在正确的文档中明确目的、边界、契约和验证；
4. **证据闭环**：设计中的关键判断有可证伪方法，运行结果能够反向校准设计和当前开发入口；
5. **持续收敛**：文档只描述现在，不积累已经失效的方案、进度流水和无当前使用者的设想。

文档体系保留完成当前目标所需的最小完整结构，使每项必要事实都有明确归属，并控制重复权威与维护成本。

## 3. 文档体系的整体结构

文档按“战略 → 当前路线 → 功能与设计 → 契约 → 验证 → 运行”的责任组织。

```text
pi-context-memory/
├── PRODUCT.md
├── ARCHITECTURE.md
├── DOCUMENTION.md
├── AGENTS.md
└── docs/
    ├── README.md
    ├── DEVELOPMENT.md
    ├── features/
    ├── system/
    ├── modules/
    ├── contracts/
    ├── validation/
    └── operations/
```

这棵树定义文档的**稳定归属空间**，不要求提前创建空目录、空索引或空文档。某个目录尚不存在，只表示当前还没有需要由该类别长期维护的事实，不表示未来没有这个归属位置。

目录一旦出现，就必须承担本文件定义的独立责任；不能因为材料较多就任意新增 `misc/`、`notes/` 或其它无法说明权威边界的分类。

## 4. 文档层级与职责

### 4.1 战略权威文档

根目录的战略文档定义项目长期目的地：

| 文档 | 唯一责任 | 相邻责任所有者 |
| --- | --- | --- |
| `PRODUCT.md` | 用户、问题、产品价值、范围、优先级和产品成功判断 | `ARCHITECTURE.md` 负责系统与模块设计；`DEVELOPMENT.md` 负责当前状态 |
| `ARCHITECTURE.md` | 系统战略目标、架构定位、模块职责、依赖方向和协作关系 | 模块与契约文档负责具体接口和数据结构；`DEVELOPMENT.md` 负责实施入口 |
| `DOCUMENTION.md` | 文档体系的战略目标、责任分区、信息流和治理规则 | 各类别文档负责具体产品与工程事实 |
| `AGENTS.md` | 为开发参与者提供最短导航和行动入口 | 战略与设计事实引用对应权威文档 |

改变产品价值或范围，只修改 `PRODUCT.md`；改变系统路线或模块责任，只修改 `ARCHITECTURE.md`；改变文档体系的分类、权威关系或治理原则，只修改 `DOCUMENTION.md`。

### 4.2 当前文档入口

`docs/README.md` 是现有文档的当前地图。它负责：

- 列出已经存在且有效的文档及其一句话责任；
- 提供按问题类型进入权威文档的导航；
- 标明尚未形成具体文档的稳定归属类别；
- 保持链接、目录状态和子目录索引一致。

完整设计由子文档承担；地图维护当前存在的文件和链接。

### 4.3 当前开发入口

`docs/DEVELOPMENT.md` 负责连接目标架构与当前实践，只维护：

- 当前可运行状态和已经成立的证据；
- 当前主导约束；
- 当前交付边界及其进入、完成条件；
- 当前唯一的下一执行入口；
- 尚未解决且会影响下一步的缺口。

它维护当前路线、状态、交付边界和执行入口。当前交付成立后，根据运行结果直接替换正文；Git 保存过去过程。

### 4.4 功能规格

`docs/features/` 保存产品边界内、用户或任务模型可以观察到的当前功能规格。功能文档回答“这个能力对使用者表现为什么”，包括：

- 目标用户场景和要改善的任务结果；
- 可观察行为、输入、输出和状态；
- 正常、失败、降级和恢复体验；
- 功能责任边界及与其它功能的关系；
- 可验收的结果及其验证入口。

功能文档服从 `PRODUCT.md`，不能自行增加产品范围、改变价值优先级，也不规定模块划分和内部实现。一个功能跨越多个模块时仍只维护一份用户可观察语义，各模块引用它。

### 4.5 跨模块系统设计

`docs/system/` 保存既定架构内、需要多个模块协同才能成立的当前战术设计。系统设计文档回答“模块怎样协作实现已经确定的能力”，包括：

- 设计目标、责任边界和所承接的架构责任；
- 跨模块控制流、数据流和状态生命周期；
- 责任分配、依赖方向和生效条件；
- 并发、失败、降级与恢复策略；
- 关键设计约束、未验证判断和证伪方式；
- 对模块文档、契约和验证文档的引用。

它不能重新划分 `ARCHITECTURE.md` 的模块责任。若设计需要改变架构所有权，应先把战略问题提交给 `ARCHITECTURE.md`。

### 4.6 模块设计

`docs/modules/` 与 `ARCHITECTURE.md` 定义的模块责任对应，保存模块当前有效的内部设计。模块文档回答“这个模块如何履行自己的责任”，包括：

- 当前设计目标和责任分工；
- 模块拥有的数据、状态、不变量和生命周期；
- 对外提供的能力、依赖和接口边界；
- 关键流程、算法选择及其理由；
- 并发、幂等、错误、降级和恢复；
- 观测点、验证方法、当前已知限制；
- 对功能、系统设计和共享契约的引用。

模块文档不复制架构战略，不拥有其它模块的数据和决策，也不把候选技术选型写成已经成立的事实。

### 4.7 独立契约

`docs/contracts/` 只保存需要被多个责任所有者共同使用、并且必须独立演进和验证的当前契约，例如外部接口、跨模块数据语义、持久化格式或兼容边界。

契约文档包括：

- 契约的提供者、消费者和所有者；
- 数据或行为语义、不变量和边界；
- 输入、输出、状态、错误和失败语义；
- 版本、兼容、迁移和废弃规则；
- 安全、权限、隔离或隐私约束；
- 契约测试和验证入口。

跨模块并不自动意味着单独建契约文档。若一个契约由单个模块拥有且不需要独立演进，应保留在提供方模块文档中，使用方只引用。

### 4.8 验证规格与证据索引

`docs/validation/` 保存证明产品结果、架构边界、功能行为、系统设计和契约是否成立的当前验证设计，包括：

- 需要回答的问题和可能推翻设计的结果；
- 基线、对照组、代表性任务和故障场景；
- fixture、checker、指标和证据完整性要求；
- 任务质量、来源恢复、完整成本和降级的判定方法；
- 可重复环境、执行入口和当前有效证据索引；
- 适用范围、限制和仍未闭合的证据缺口。

验证文档定义如何证明，不保存按日期累积的运行报告。机器可读原始结果属于验证基础设施；长期文档只索引仍支撑当前结论的最小证据。新的验证替代旧结论后，应更新权威结论并移除失效引用。

### 4.9 运行与维护文档

`docs/operations/` 保存已经可用行为的安装、配置、运行和故障处理说明，面向实际使用者和维护者，包括：

- 支持版本、安装、启用、升级、禁用和卸载；
- 配置项、默认值、优先级和敏感信息处理；
- 运行状态、观测方式和诊断入口；
- 正常、降级、恢复和数据处理流程；
- 兼容性、权限、安全、备份和清理边界；
- 可执行命令及其预期结果。

运行文档只能描述当前真实可用并经过验证的行为。尚未实现的运行目标应进入功能、系统或模块设计，不能提前写成用户操作说明。

### 4.10 源码、测试与生成内容

源码和测试是可执行行为的权威来源，但不能替代产品、架构和设计意图。文档定义“为什么、边界是什么、如何验证”，代码和测试证明“当前实现实际做了什么”。

由 schema、类型或 CLI 自动生成的参考内容应由生成源负责，不手工维护重复副本。生成内容只有在有明确消费者、稳定入口和自动更新路径时才进入仓库。

## 5. 权威信息流

文档之间遵循以下依赖方向：

```text
PRODUCT.md
  └── docs/features/
        └── docs/validation/

PRODUCT.md
  └── ARCHITECTURE.md
        ├── docs/system/
        ├── docs/modules/
        └── docs/contracts/
              └── docs/validation/

已验证的功能与实现
  └── docs/operations/

以上所有当前状态与证据
  └── docs/DEVELOPMENT.md

DOCUMENTION.md
  └── 约束全部文档的归属、权威关系和生命周期
```

上层文档定义下层文档必须满足的目的和边界；下层运行证据可以证明设计不足，但不能在局部悄悄改变上层战略。若实践要求改变产品、架构或文档体系，应把证据和取舍提交到对应战略文档，由该层做决定。

## 6. 新建或更新文档的判断

### 6.1 先判断事实归属

出现新信息或变化时，按下列问题定位：

| 问题 | 权威位置 |
| --- | --- |
| 为什么做、为谁做、产品是否应该做 | `PRODUCT.md` |
| 系统有哪些责任、谁拥有它、模块怎样依赖 | `ARCHITECTURE.md` |
| 文档体系如何分类和治理 | `DOCUMENTION.md` |
| 当前做到哪里、证据是什么、下一步是什么 | `docs/DEVELOPMENT.md` |
| 用户或任务模型会观察到什么行为 | `docs/features/` |
| 多个模块怎样共同实现一个已确定能力 | `docs/system/` |
| 单个模块内部怎样履行责任 | `docs/modules/` |
| 多方共同依赖且需独立演进的语义是什么 | `docs/contracts/` |
| 怎样证明行为、质量、成本和边界成立 | `docs/validation/` |
| 当前可用能力怎样安装、配置、运行和恢复 | `docs/operations/` |
| 实际执行行为是什么 | 源码与测试 |

如果一个事实找不到归属，先检查它是否真的具有当前使用者和长期责任；不要用新增杂项目录掩盖尚未形成的责任。

### 6.2 优先更新，谨慎新建

先查找是否已有文档拥有该事实。现有文档能够完整承担责任时，直接更新，不另建“新版”“补充”“最终版”或按日期命名的竞争文档。

只有同时满足以下条件才新建文档：

1. 它承担一个独立、长期且当前需要的责任；
2. 能指出当前读者或消费者；
3. 与已有文档的权威边界清楚且没有重复；
4. 有验证、校准或维护路径；
5. 如果删除它，会使必要事实失去唯一归属或显著增加理解成本。

目录第一次承载实际正文或需要维护当前稳定映射时，同时创建该目录的 `README.md` 作为索引；不为没有当前责任的空目录创建索引。

### 6.3 文档先于实现

当变化涉及用户可观察行为、数据语义、模块责任、跨模块流程、外部契约、失败降级或验证方式时，顺序是：

1. 确认产品和架构战略是否允许该变化；
2. 在正确类别中创建或更新功能、系统、模块或契约文档；
3. 在验证文档中明确预期结果、可证伪结果和证据入口；
4. 实现当前边界内的最小完整交付；
5. 通过运行结果校准设计，删除不成立的判断；
6. 行为可用后更新运行文档；
7. 更新 `DEVELOPMENT.md` 的当前状态、交付边界和下一入口。

“文档先于实现”不等于把猜测写成事实。实现前文档应明确战略约束、当前设计决定、尚未验证的判断及其验证方法；实现后只保留被当前设计和证据支持的内容。

## 7. 每类文档的共同内容要求

除索引文档外，每份设计、契约、验证或运行文档至少应使读者能够回答：

1. **文档角色**：它拥有什么事实，服从和被谁引用；
2. **目的与边界**：它要达到什么结果，相邻责任由谁承担；
3. **当前内容**：当前有效的行为、设计、契约或方法是什么；
4. **关键约束**：哪些安全、质量、成本、隔离或兼容边界不能被破坏；
5. **关联关系**：它依赖哪些权威文档，向哪些文档或实现提供约束；
6. **验证与校准**：怎样证明当前内容成立，什么结果会要求修改它；
7. **未闭合边界**：哪些判断尚未验证或当前仍不成立。

文档直接陈述当前事实和理由。只有无法从结构推导、且会影响未来取舍的原因才保留说明；不记录讨论过程和个人观点。

## 8. 索引、命名与链接

- `docs/README.md` 维护全局当前索引；每个已存在的子目录用自己的 `README.md` 维护局部映射；
- 文档名称表达长期责任，使用稳定、可检索的语义名称，不使用日期、人员、阶段编号、`new`、`final` 或版本形容词；
- 一个主题只有一个主文档；内容增长时按责任拆分，不按篇幅拆分；
- 使用相对链接指向权威来源，不复制上层章节全文；
- 子目录索引说明每份文档的责任、状态和读者，不重复正文；
- 文件移动、重命名或删除时，同一次变更更新所有入口和引用；
- 需要并存多个协议版本时，只有真实兼容期内才允许版本化文件，兼容期结束后删除旧版本及入口。

## 9. 文档更新触发条件

| 变化 | 必须同步更新 |
| --- | --- |
| 产品价值、范围、优先级或成功判断变化 | `PRODUCT.md`，以及受影响的功能和验证文档 |
| 模块责任、依赖方向或系统路线变化 | `ARCHITECTURE.md`，以及受影响的系统、模块和契约文档 |
| 用户可观察行为变化 | 对应功能、验证和运行文档 |
| 数据、接口、流程或失败语义变化 | 对应系统、模块、契约和验证文档 |
| 安装、配置、诊断或恢复方式变化 | 对应运行文档 |
| 验证结果推翻当前设计 | 先更新拥有该判断的设计文档，再更新验证结论和当前开发入口 |
| 当前主导约束或交付边界变化 | `docs/DEVELOPMENT.md` |
| 文档分类或治理变化 | `DOCUMENTION.md`、`docs/README.md` 和受影响索引 |

代码变更只有在不改变任何已有文档事实、边界和验证方式时，才可以不修改正文；仍应确认索引和引用没有失效。

## 10. 删除、合并与替换

文档在承担当前责任期间保留。出现以下情况时应删除或合并：

- 所描述的责任已经从产品或架构中消失；
- 当前事实已由另一个权威位置完整承担；
- 没有当前读者、消费者或验证路径；
- 只记录已经结束的讨论、实验或迁移过程；
- 内容可以由源码、schema 或工具可靠生成；
- 文档长期只能通过复制其它文档维持。

删除前先把仍然有效且必要的事实迁移到正确权威位置，并更新所有引用。历史由 Git 保存，不为被替代文档建立 `archive/`。

## 11. 不进入长期文档体系的内容

以下内容默认不作为长期权威文档保留：

- 按日期累积的周报、进度日志和完成清单；
- 会议记录、讨论原文和没有形成当前约束的 ADR/RFC 流水；
- 已淘汰方案、候选技术清单和“以后可能需要”的设想；
- 临时调查笔记、一次性调试输出和可重新生成的报告；
- 与源码重复、靠人工同步的 API 或配置参考；
- 没有责任定义的 `misc/`、`notes/`、`research/`、`plans/`、`decisions/`、`archive/`。

调查形成的当前技术事实应进入拥有该事实的系统、模块、契约、验证或运行文档；没有继续影响当前决策的过程材料应删除。

## 12. 冲突与决策权

发生文档冲突时，按责任归属判断：

1. 产品事实服从 `PRODUCT.md`；
2. 系统责任和依赖服从 `ARCHITECTURE.md`；
3. 文档归属和治理服从 `DOCUMENTION.md`；
4. 功能、系统、模块、契约、验证和运行事实服从各自唯一责任所有者；
5. 可执行行为冲突时先用运行结果确认实际状态，再校准错误的实现或文档；
6. `DEVELOPMENT.md` 只能报告当前状态，不能覆盖上述权威设计。

改变产品、架构或文档体系目的地属于战略决策，由用户决定。既定战略内如何组织具体功能、系统、模块、契约和验证文档属于战术执行，应依据当前责任和实践证据自主完成。

## 13. 文档架构评价

判断文档体系是否有效，不看文件数量，而看新的维护者能否：

1. 从产品目标找到对应功能和验收方法；
2. 从架构模块找到跨模块设计、模块内部设计和共享契约；
3. 从一项设计找到证明它成立的验证入口和当前证据；
4. 从可用能力找到准确的安装、配置、诊断和降级说明；
5. 从 `DEVELOPMENT.md` 明确当前状态、主导约束和唯一下一入口；
6. 对任一当前事实只找到一个权威答案；
7. 在变化发生时清楚知道应更新、创建、合并或删除哪份文档。

上述路径断裂表示必要文档责任缺失；同一问题存在多个答案表示文档边界失真。两种情况都应修正文档结构和权威关系。
